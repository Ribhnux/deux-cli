<?php
/**
 * {{helper.name}}
 *
 * @package {{theme.name}}
 * @since {{theme.version}}
 */

if ( ! function_exists( '{{theme.slugfn}}_{{helper.slugfn}}' ) ) :
	/**
	 * {{helper.description}}
	 *
	 * @return void
	 */
	function {{theme.slugfn}}_{{helper.slugfn}}() {

	}
endif;
