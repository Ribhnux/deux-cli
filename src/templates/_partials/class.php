<?php
/**
 * Class Name: {{lib.name}}
 *
 * @package {{theme.name}}
 * @since {{theme.version}}
 */

if ( ! class_exists( '{{lib.className}}' ) ) :

	/**
	 * {{lib.description}}
	 *
	 * @since {{theme.version}}
	 * @package {{theme.name}}
	 * @author  {{theme.author}}
	 */

	class {{lib.className}} {
		public function __construct() {

		}
	}
endif;
