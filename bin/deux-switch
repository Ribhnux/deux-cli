#!/usr/bin/env node

require('babel-register')
const program = require('commander')
const cli = require('../src/cli')

program.parse(process.argv)

cli()
  .then(command => {
    command.switch(program.args)
  })
  .catch(() => {
    process.exit(0)
  })
